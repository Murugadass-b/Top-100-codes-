#include<stdio.h>
#include<math.h>
int main(){
    int n;
    scanf("%d",&n);
    int a = 0;
    for(int i=2;i<=sqrt(n);i++){
        if(n%i==0)
            a++;
    }
    printf((a==0)?"Prime":"Not a Prime");
}
